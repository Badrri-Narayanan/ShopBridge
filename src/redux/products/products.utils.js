const fetchProductsFromServer = () => {
    return product_list
}

//export default fetchProductsFromServer;
    
// if(localStorage.getItem('cartItems')) {
//     this.setState({cartItems: JSON.parse(localStorage.getItem('cartItems'))});
// }
